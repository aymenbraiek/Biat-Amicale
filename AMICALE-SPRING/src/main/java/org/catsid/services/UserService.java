package org.catsid.services;

//import org.catsid.dao.UserRepository;
//import org.catsid.entities.VisionAdmin;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;


/** 
 * @author 
 *
 */
@Service
public class UserService {

	/*
	@Autowired
	UserRepository userRepository;

	public VisionAdmin save(VisionAdmin user) {
		return userRepository.saveAndFlush(user);
	}

	public VisionAdmin update(VisionAdmin user) {
		return userRepository.save(user);
	}

	
	
	public VisionAdmin authentification(String username) {
		return userRepository.findUser(username);
	}
	
	public VisionAdmin authentificationLDAP(String username, String password) {
		return userRepository.findUserLDAP(username);
	}
	*/
}
